
//旋转按钮脚本
$(function(){

	// 初始化脚本
	var sigknob0 = {   //定于两个坐标值比较
		x:0,
		y:0,
		tan:0
	}
	var sigknob1 = {
		x:0,
		y:0,
		tan:0
	}

	// 鼠标离开任意一个子元素或所选元素触发
	function sigMouseout(event){
		var sigknob.x = event.pageX;
		var sibknob.y = event.pageY;
		var sigknob.tan = sibknob.y/sibknob.x;
		if(sigknob.x*){
			
		}
	}
})